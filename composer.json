{
    "name": "ajaminov/trip-sorter",
    "description": "Demo package for sorting cards.",
    "minimum-stability": "dev",
    "require": {
        "php": ">=7.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^6"
    },
    "autoload": {
        "psr-4": {
            "TripSorter\\": "src/"
        }
    },
    "scripts": {
        "test": "vendor/bin/phpunit --colors=always",
        "execute": "php trip-sorter.php"
    }
}