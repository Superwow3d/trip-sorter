{
    "name": "ajaminov/trip-sorter",
    "description": "Demo package for sorting cards.",

    "require": {
        "php": ">=7.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^6"
    },
    "autoload": {
        "psr-4": {
            "TripSorter\\": "src/"
        }
    },
    "scripts": {
        "test": "vendor/bin/phpunit --colors=always"
    }
}